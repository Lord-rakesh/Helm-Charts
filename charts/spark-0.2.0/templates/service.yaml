apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-0.2.0.fullname" . }}
  labels:
    {{- include "spark-0.2.0.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark-0.2.0.selectorLabels" . | nindent 4 }}
